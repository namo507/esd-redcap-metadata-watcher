# Legacy NANO output compatibility

These undated NANO artifacts are retained for links and workflows that expect
the historical filenames. They are regenerated by `recruitment_reports.py`
from the same canonical data and logic used for `recruitment_outputs/`.

Do not run a separate NANO-only calculation or edit these files by hand. Use
the dated canonical workbook in `recruitment_outputs/` for provenance and QA.
