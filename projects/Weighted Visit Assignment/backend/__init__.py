"""HTTP backend for the ESD Visitboard.

Pure standard library, exactly like the engine it wraps. There is nothing to
install, no build step and no lockfile, which is the only reliable way to make
"works without fail" true for a tool a small lab runs a few times a week.
"""

__version__ = "1.0.0"
